/**
 * Peter Fight
 *
 * Enum con todos los tipos de mapa que gestiona la aplicación. Son útiles para crear instancias en la clase abstracta
 */

enum tiposMaps{
  MUERTES_X_FECHA,
  CASOS_X_FECHA,
  MUERTES_ACUMULADAS_X_FECHA,
  CASOS_ACUMULADOS_X_FECHA
}

export default tiposMaps;